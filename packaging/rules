#!/usr/bin/make -f
%:
		dh $@

override_dh_shlibdeps:
	dh_shlibdeps --dpkg-shlibdeps-params=--ignore-missing-info

override_dh_auto_install:
	$(MAKE) DESTDIR=$$(pwd)/debian/snap4arduino prefix=/usr install
